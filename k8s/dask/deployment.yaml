---
# Dask Scheduler Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dask-scheduler
  namespace: tpch-benchmark
  labels:
    app: dask-benchmark
    component: scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dask-benchmark
      component: scheduler
  template:
    metadata:
      labels:
        app: dask-benchmark
        component: scheduler
    spec:
      serviceAccountName: dask-sa
      volumes:
      - name: output-volume
        persistentVolumeClaim:
          claimName: tpch-output-pvc
      containers:
      - name: dask-scheduler
        image: gabrielranulfo/tpch-benchmark:dask-latest
        imagePullPolicy: IfNotPresent
        command: ["python", "-m", "distributed.cli.dask_scheduler"]
        ports:
        - name: tcp
          containerPort: 8786
        - name: http
          containerPort: 8787
        resources:
          requests:
            cpu: "1"
            memory: "2Gi"
          limits:
            cpu: "2"
            memory: "4Gi"
        livenessProbe:
          httpGet:
            path: /status
            port: 8787
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /status
            port: 8787
          initialDelaySeconds: 10
          periodSeconds: 5

---
# Dask Scheduler Service
apiVersion: v1
kind: Service
metadata:
  name: dask-scheduler
  namespace: tpch-benchmark
  labels:
    app: dask-benchmark
    component: scheduler
spec:
  type: ClusterIP
  selector:
    app: dask-benchmark
    component: scheduler
  ports:
  - name: tcp
    port: 8786
    targetPort: 8786
  - name: http
    port: 8787
    targetPort: 8787

---
# Dask Driver Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dask-driver
  namespace: tpch-benchmark
  labels:
    app: dask-benchmark
    component: driver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dask-benchmark
      component: driver
  template:
    metadata:
      labels:
        app: dask-benchmark
        component: driver
    spec:
      serviceAccountName: dask-sa
      volumes:
      - name: output-volume
        persistentVolumeClaim:
          claimName: tpch-output-pvc
      - name: data-volume
        persistentVolumeClaim:
          claimName: tpch-data-pvc
      containers:
      - name: dask-driver
        image: gabrielranulfo/tpch-benchmark:dask-latest
        imagePullPolicy: IfNotPresent
        command: ["python", "-m", "queries.dask"]
        env:
        - name: RUN_DASK_SCHEDULER
          value: "tcp://dask-scheduler:8786"
        - name: RUN_LOG_TIMINGS
          value: "1"
        - name: SCALE_FACTOR
          value: "50"
        volumeMounts:
        - name: output-volume
          mountPath: /root/polars-benchmark/output
        - name: data-volume
          mountPath: /root/polars-benchmark/data/tables
        resources:
          requests:
            cpu: "1"
            memory: "4Gi"
          limits:
            cpu: "2"
            memory: "8Gi"
        livenessProbe:
          exec:
            command: ["python", "--version"]
          initialDelaySeconds: 30
          periodSeconds: 10

---
# Dask Workers Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dask-worker
  namespace: tpch-benchmark
  labels:
    app: dask-benchmark
    component: worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dask-benchmark
      component: worker
  template:
    metadata:
      labels:
        app: dask-benchmark
        component: worker
    spec:
      serviceAccountName: dask-sa
      volumes:
      - name: output-volume
        persistentVolumeClaim:
          claimName: tpch-output-pvc
      - name: data-volume
        persistentVolumeClaim:
          claimName: tpch-data-pvc
      containers:
      - name: dask-worker
        image: gabrielranulfo/tpch-benchmark:dask-latest
        imagePullPolicy: IfNotPresent
        command: ["python", "-m", "distributed.cli.dask_worker", "dask-scheduler:8786"]
        env:
        - name: RUN_DASK_SCHEDULER
          value: "tcp://dask-scheduler:8786"
        - name: RUN_LOG_TIMINGS
          value: "1"
        args:
        - "--nthreads=1"
        - "--memory-limit=7Gi"
        - "--dashboard"
        volumeMounts:
        - name: output-volume
          mountPath: /root/polars-benchmark/output
        - name: data-volume
          mountPath: /root/polars-benchmark/data/tables
        resources:
          requests:
            cpu: "2"
            memory: "4Gi"
          limits:
            cpu: "4"
            memory: "8Gi"
        livenessProbe:
          exec:
            command: ["python", "--version"]
          initialDelaySeconds: 30
          periodSeconds: 10
